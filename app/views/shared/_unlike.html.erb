
<%= form_for(user_image.likes.find_by(user_id: current_user.id),
                              html: {method: :delete}, remote: true) do |f| %>
  
  <%= f.submit 'いいね済み', class: "btn btn-primary" %>

<% end %>